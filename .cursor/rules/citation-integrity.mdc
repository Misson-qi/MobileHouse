---
description: 引用真实性、数字核验与来源边界
alwaysApply: true
---

# 引用与来源完整性

本仓库当前工作重心是《移动住房时代，凤凰来仪》相关内容，本规则默认作为全项目通用约束。

## 核心原则

- 引用的资料、数据和出处必须 `真实、有效、可验证`。
- 不得虚构数据、虚构来源、拼接不存在的结论。
- 不得把二手转述伪装成一手来源。
- 只要文本涉及 `数字`、`年份`、`比例`、`成本`、`时间节点`、`数量级` 或 `排名比较`，默认先检查来源，再决定是否保留或改写。

## 来源优先级

- 默认按以下顺序判断：`政府/标准/学术论文/企业年报` > `权威机构报告` > `可追溯的正式媒体报道`。
- 自媒体、转引汇总、无原始出处网页，不足以单独支撑核心事实、关键数据或强结论。
- 已存在且可核验的真实来源，不要随意删除、替换或弱化；除非新来源更准确，或原引用确有错误。

## 使用要求

- 引用时尽量给出：机构名、文献名、发布时间、链接或检索线索，确保他人可以复核。
- 不得把来源中的相关结论扩写成更强的结论；必须忠实于原始材料的适用范围、时间范围和条件边界。
- 如果来源只能支撑局部结论，就只写到该结论为止，不外推成行业整体判断或长期预测。
- 用户未明确要求扩写时，不要自行补充未经核验的新数据、新案例或新出处。

## 默认动作

- 若关键事实或数字缺少可靠来源，默认动作是：`不补写`、`改成审慎表述`、或标记 `[待核实：来源/年份/链接]`。
- 处理事实性、数据性、政策性内容时，默认顺序是：`先核来源` -> `再写判断` -> `最后润色文风`。
- 如果只能确认大致趋势、不能确认精确数字，改写为趋势判断，不保留具体数值。

## 模板

- 正文内简引推荐写法：`（来源：机构名，文献/报告名，年份）`
- 带链接写法：`（来源：机构名，《文献名》，YYYY-MM-DD，链接）`
- 待核实写法：`[待核实：具体数据项的来源/年份/链接]`
